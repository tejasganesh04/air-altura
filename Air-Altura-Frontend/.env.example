# Set this to your API Gateway's public URL at build time on Vercel/Netlify.
# Leave unset in local dev — the Vite proxy handles it automatically.
VITE_API_URL=https://your-api-gateway.up.railway.app
